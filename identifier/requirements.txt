fastapi==0.115.6
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84

# GPU-capable OCR (EasyOCR is the primary OCR engine)
# Install with CUDA support on production:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install easyocr
# CPU-only OCR (development/testing):
#   pip install torch torchvision easyocr
# EasyOCR depends on torch; keep versions aligned.
torch>=2.2.0
torchvision>=0.17.0

# OCR backends
EasyOCR>=1.7.1

# Database
sqlalchemy>=2.0.0

# Test dependencies
pytest>=8.0.0
httpx>=0.27.0
